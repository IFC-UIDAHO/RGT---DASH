# ── Python ──
venv/
venv_*/
.venv/
env/
setup_*.log
__pycache__/
*.pyc
*.pyo

# ── Secrets — NEVER commit ──
.env
api_key.txt

# ── OS / editor cruft ──
.DS_Store
Thumbs.db
*.bak
*.swp

# ── Generated at runtime (rebuilt on every startup) ──
assets/installations_map.html

# stray test probes (sandbox could not delete)
_probe*.txt

# data pipeline runtime artifacts
tools/__pycache__/
tools/auto_update.log
tools/auto_update.state.json
data_inbox/processed/

# (_rgt_serve.py is now a tracked source file — the Windows shared-server entry point)

# raw workbooks dropped for updates (not source code)
data_inbox/*.xlsx

# rolling data backup (safety)
data/*.prev.csv
old_vs_new_changes.csv
