# ── Python ──
venv/
venv_*/
.venv/
env/
setup_*.log
__pycache__/
*.pyc
*.pyo

# ── Secrets — NEVER commit ──
.env
api_key.txt

# ── OS / editor cruft ──
.DS_Store
Thumbs.db
*.bak
*.swp

# ── Generated at runtime (rebuilt on every startup) ──
assets/installations_map.html

# stray test probes (sandbox could not delete)
_probe*.txt

# generated by the launcher's shared-server mode
_rgt_serve.py
