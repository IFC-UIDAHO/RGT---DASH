# RGT dashboard v2 — runtime dependencies (lean).
# The legacy requirements (geopandas/laspy/open3d/etc.) were unrelated to the
# dashboard and are preserved in requirements-legacy.txt.
dash>=2.16,<3
dash-bootstrap-components>=1.5
plotly>=5.18
pandas>=2.0
numpy>=1.24
scipy>=1.10
requests>=2.28
python-dotenv>=1.0   # silences Flask .env tip; .env still loads without it
gunicorn>=21.0   # optional: production WSGI server
